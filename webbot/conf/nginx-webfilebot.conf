# WebFileBot 公司网站 — www → 8004 (公司简介)
server {
    listen 443 ssl http2;
    server_name www.webfilebot.com webfilebot.com;

    ssl_certificate     /etc/nginx/ssl/filebot-cert.pem;
    ssl_certificate_key /etc/nginx/ssl/filebot-key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    allow 127.0.0.1;
    allow 10.0.0.0/8;
    allow 172.16.0.0/12;
    allow 192.168.0.0/16;
    allow 174.112.193.81;
    deny all;

    location / {
        proxy_pass http://127.0.0.1:8004;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Canada.ca 内容 — ca → 8003 (Publish Server)
server {
    listen 443 ssl http2;
    server_name ca.webfilebot.com;

    ssl_certificate     /etc/nginx/ssl/filebot-cert.pem;
    ssl_certificate_key /etc/nginx/ssl/filebot-key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    allow 127.0.0.1;
    allow 10.0.0.0/8;
    allow 172.16.0.0/12;
    allow 192.168.0.0/16;
    allow 174.112.193.81;
    deny all;

    location / {
        proxy_pass http://127.0.0.1:8003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 生产环境 — prod → 5080 (WebBot + FileBot + Vite)
server {
    listen 443 ssl http2;
    server_name prod.webfilebot.com;

    ssl_certificate     /etc/nginx/ssl/filebot-cert.pem;
    ssl_certificate_key /etc/nginx/ssl/filebot-key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    allow 127.0.0.1;
    allow 10.0.0.0/8;
    allow 172.16.0.0/12;
    allow 192.168.0.0/16;
    allow 174.112.193.81;
    deny all;

    location / {
        proxy_pass http://127.0.0.1:5080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
